# Use the official Microsoft ASP.NET runtime as a base image.
# This creates a lean Linux container with everything needed to run a compiled .NET app.
FROM mcr.microsoft.com/dotnet/aspnet:8.0

# Set the working directory inside the container to /app
WORKDIR /app

# At a later stage, you will add commands here to copy your compiled code in.
# For now, this creates a valid, runnable container ready for your application.

